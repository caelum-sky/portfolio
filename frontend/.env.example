# Copy to .env.local for local dev
# In production this is injected by Firebase / GitHub Actions

# Your Render backend URL (no trailing slash)
VITE_BACKEND_URL=https://your-portfolio-backend.onrender.com
